package haituntiandi.com.haituntiandi.app.{$name};

import android.app.Activity;

public class {$Name}Presenter implements {$Name}Contract.Presenter {
    private {$Name}Contract.View rootView;

    private Activity context;

    public {$Name}Presenter({$Name}Contract.View rootView) {
        this.rootView = rootView;
        this.context = rootView.getActivity();
    }
}
